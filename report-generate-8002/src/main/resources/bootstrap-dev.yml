server:
  port: 8002

spring:
  application:
    name: report-generate
  devtools:
    livereload:
      port: 35272

  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        group: dev
        namespace: backend-dev
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
        group: dev
        namespace: backend-dev