server:
  port: 8003

spring:
  application:
    name: report-login
  devtools:
    livereload:
      port: 35273

  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        group: dev
        namespace: backend-dev
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
        group: dev
        namespace: backend-dev