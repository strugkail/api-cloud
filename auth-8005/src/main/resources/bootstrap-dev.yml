server:
  port: 8005

spring:
  application:
    name: auth
  devtools:
    livereload:
      port: 35656

  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        group: dev
        namespace: backend-dev
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
        group: dev
        namespace: backend-dev